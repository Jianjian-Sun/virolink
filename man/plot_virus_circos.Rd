% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/virus_circos.R
\name{plot_virus_circos}
\alias{plot_virus_circos}
\title{Plot a virus genome with concentric circos tracks}
\usage{
plot_virus_circos(
  virus_length,
  virus_name = "virus",
  features = NULL,
  tracks = NULL,
  fill = NULL,
  height = 0.12,
  label = TRUE,
  label_cex = 0.55,
  label_col = "grey20",
  label_min_width = 250,
  border_col = NA,
  show_virus_name = TRUE,
  track_margin = c(0.004, 0.004),
  start.degree = 90,
  gap.degree = 0,
  clear = TRUE,
  draw = TRUE
)
}
\arguments{
\item{virus_length}{Virus genome length in base pairs.}

\item{virus_name}{Virus sequence name used for the sector label.}

\item{features}{Optional virus feature intervals.}

\item{tracks}{Optional list of \code{vi_virus_circos_track} objects.}

\item{fill}{Default feature fill color, unnamed palette, or named vector by
feature name or feature type. When \code{NULL}, each distinct feature name
gets its own color.}

\item{height}{Track height for the gene layer created from \code{features}.}

\item{label}{Logical. Whether to label gene blocks.}

\item{label_cex}{Gene label text size.}

\item{label_col}{Gene label color.}

\item{label_min_width}{Minimum feature width, in base pairs, required for a
gene label to be drawn.}

\item{border_col}{Gene block border color. Defaults to \code{NA} so split
circular features do not show a seam at the origin.}

\item{show_virus_name}{Logical. Whether to draw the virus name in the center
of the circular plot.}

\item{track_margin}{Spacing between concentric tracks.}

\item{start.degree}{Starting angle for the circular layout.}

\item{gap.degree}{Gap between sectors. With one sector, keep this at zero.}

\item{clear}{Logical. Whether to clear the current circlize device first.}

\item{draw}{Logical. Whether to draw immediately.}
}
\value{
A \code{vi_virus_circos_plot} object.
}
\description{
The plot initializes a single circular sector whose length equals the virus
genome length. Feature intervals are laid out from the outermost ring inward.
All intervals with the same feature name stay on one ring, which keeps split
records for a circular virus gene visually connected across the origin.
Overlapping feature groups are pushed to the next inner ring. Labels are
centered in the colored blocks when the interval is wide enough, including
across-origin centers for split circular features; short labels are omitted
and reported with a warning.
}
